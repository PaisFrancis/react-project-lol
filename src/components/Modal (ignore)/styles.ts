/* import styled from "styled-components";
import Champion from "../../pages/Champions/types";

export interface ChampionModalProps {
  show: boolean;
  handleClose: () => void;
  champion: Champion;
  name: string;
  title: string;
  blurb: string;
  imageUrl: string;
}

export const ModalWrapper = styled.div`
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  background-color: rgba(0, 0, 0, 0.3);
  z-index: 9999;
`;

export const ModalBox = styled.div`
  background-color: white;
  padding: 2rem;
  border-radius: 0.5rem;
  box-shadow: 0px 2px 4px rgba(0, 0, 0, 0.25);
`;

export const CloseButton = styled.button`
  position: absolute;
  top: 0;
  right: 0;
  margin: 1rem;
  border: none;
  background: none;
  font-size: 2rem;
  cursor: pointer;
`;

export const ChampionName = styled.h2`
  font-size: 3rem;
  font-weight: 700;
  margin-bottom: 1rem;
`;

export const ChampionTitle = styled.h3`
  font-size: 2rem;
  font-weight: 500;
  margin-bottom: 1rem;
`;

export const ChampionBlurb = styled.p`
  font-size: 1.5rem;
  font-weight: 400;
  margin-bottom: 2rem;
`;

export const ChampionImage = styled.img`
  width: 100%;
  height: auto;
  border-radius: 0.5rem;
`;
 */
